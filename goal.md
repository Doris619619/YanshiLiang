# Personal Website Goal Queue

> 项目目标：基于用户随后提供的真实简历、项目资料与个人素材，开发一个可长期维护的个人作品集网站。
>
> **唯一首要视觉参考：<https://innei.in/> 及其 Yohaku（余白）设计语言。**
>
> 本项目追求对参考站点在**字体气质、字号层级、留白节奏、灰阶、排版密度、导航状态、轻量动效和整体氛围**上的高保真实现。不得退化为普通简历模板、SaaS 落地页、霓虹科技风或卡片拼贴站。

---

## 0. 执行总则

1. 在开始编码前，必须完整阅读：
   - 用户提供的简历；
   - 用户提供的头像、项目图、学校/交换照片等素材；
   - 当前仓库中的 `README`、`AGENTS.md`、现有设计系统和已有代码；
   - `https://innei.in/` 当前线上页面；
   - `https://github.com/Innei/Yohaku` 中公开的设计系统文档，尤其是 `design-system/CHEATSHEET.md`。
2. 参考站点不是“灵感之一”，而是本项目的**主视觉标杆**。所有关键视觉决策都必须能说明它与该标杆的关系。
3. 可以高保真复现公开设计语言，但不得复制对方的：
   - 私有源代码；
   - 原文、个人简介、文章内容；
   - 头像、照片、Logo、图标资产；
   - 具有明确个人归属的插画和品牌标识。
4. 允许并鼓励使用 Yohaku 在 MIT 许可范围内公开的设计 token、结构思想和实现方式；如直接使用其公开代码，必须保留对应许可证与 attribution。
5. 所有个人信息必须来自用户提供的资料。不得虚构奖项、数字、学校、职位、项目结果、合作方、论文状态或社交链接。
6. 未获得的信息写入 `content/todo.ts` 或等价的集中式 TODO 数据文件，不得散落假文案。
7. 所有 Goal 必须在**同一次任务中连续执行**。某个 Goal 未通过时，应立即在当前任务内返工，直到通过后自动进入下一个 Goal；不得停下来等待用户确认，不得只汇报阶段结果。
8. 每个 Goal 完成后必须在内部完成：
   - 运行测试和构建；
   - 生成桌面端与移动端截图；
   - 按验收标准逐项自检；
   - 记录变更、未通过项和下一步；
   - 对未通过项直接返工，不只输出问题清单；
   - 验收通过后立即继续执行后续 Goal，不向用户索要阶段性确认。

---

# Goal 0 — 建立“几乎不可误认”的 Yohaku 视觉基线

## Goal 定义

在不复制 Innei 私人内容和私有实现的前提下，建立一套高度接近 `innei.in` 当前视觉语言的本地设计系统，并完成一个仅使用真实或明确占位内容的首页视觉原型。

这是一个故意设定得很高的门槛：

> 当参考站点与本项目首页以相同视口并排展示时，熟悉 `innei.in` 的观察者应能立即判断两者属于同一类“文字优先、纸张感、低噪声、强留白、细字体层级”的设计语言；同时又能清楚看出内容和个人身份属于用户本人，而不是 Innei 的镜像站。

## 必须实现

### A. 字体系统——本 Goal 最重要的验收项

必须建立且只建立以下字体角色：

```css
--font-sans
--font-serif
--font-mono
--font-logo-cjk
--font-logo-latin
```

推荐字体栈：

```css
--font-sans:
  Inter, ui-sans-serif, system-ui, -apple-system,
  BlinkMacSystemFont, "Segoe UI", "PingFang SC",
  "Microsoft YaHei", "Noto Sans SC", "Hiragino Sans GB",
  Arial, sans-serif;

--font-serif:
  "Noto Serif CJK SC", "Source Han Serif SC",
  "Songti SC", STSong, Georgia, serif;

--font-mono:
  "Cascadia Code", "JetBrains Mono", "Fira Code",
  Consolas, Monaco, "Noto Sans Mono CJK SC", monospace;

--font-logo-cjk:
  "Noto Serif JP", "Source Han Serif SC",
  "Noto Serif CJK SC", serif;

--font-logo-latin:
  "EB Garamond", "Cormorant Garamond",
  Georgia, serif;
```

执行要求：

1. 中文正文必须有可靠 CJK fallback。
2. 中文标题禁止使用 `font-bold`；默认使用 `font-medium` 或正常字重。
3. 正文默认 14px，不能擅自放大成常见模板的 16–18px。
4. 全局默认字距约为 `0.01em`。
5. 标题、正文、注释、日期必须形成明显但克制的字体材质差异。
6. 日期、脚注、短注释允许使用斜体 serif，形成类似纸张边注的感觉。
7. Logo 专用字体不得用于正文或普通 UI。
8. 不允许随意引入第四种正文/UI 字体角色。
9. 不允许使用 Tailwind 默认 `text-sm/text-base/text-xl/...` 作为最终排版规范。
10. 不允许在组件内散落 `text-[13px]` 之类硬编码字号。

必须定义以下字号 token：

| Token | 字号 | 行高 | 用途 |
|---|---:|---:|---|
| `caption-10` | 10px | 14px | 极少量 eyebrow |
| `label-12` | 12px | 18px | 日期、标签、脚注 |
| `copy-13` | 13px | 20px | 卡片或紧凑描述 |
| `copy-14` | 14px | 22px | 默认正文 |
| `copy-15` | 15px | 24px | 强调正文、文章正文 |
| `copy-16` | 16px | 26px | 大正文 |
| `title-20` | 20px | 28px | 区块标题 |
| `title-24` | 24px | 32px | 次级页面标题 |
| `title-28` | 28px | 36px | 页面 H1 |
| `display-36` | 36px | 44px | Hero 主标题 |
| `display-48` | 48px | 56px | 极少量大展示标题 |

### B. 色彩与材质

建立三层中性灰系统：

- `neutral-1` 至 `neutral-4`：页面和填充；
- `neutral-5` 至 `neutral-7`：边框、图标、次要文本；
- `neutral-8` 至 `neutral-10`：正文和标题。

浅色模式应接近纸张，而不是纯白科技页面：

```css
--surface-paper: #fefefb;
--neutral-1: #f9f8f5;
--neutral-2: #f0efeb;
--neutral-3: #e3e1db;
--neutral-4: #d0cec6;
--neutral-5: #a8a69f;
--neutral-6: #787670;
--neutral-7: #5c5a55;
--neutral-8: #403f3a;
--neutral-9: #24231f;
--neutral-10: #141312;
```

要求：

1. 深色模式必须是暖灰夜色，不能是纯黑。
2. 强调色只能覆盖任一视图面积的约 5% 以内。
3. 强调色只用于 CTA、焦点环、品牌记号、引用边线或极少量内容强调。
4. 禁止大面积蓝紫渐变、霓虹发光、彩色背景块。
5. 禁止硬投影；层次依赖 1px ring、极轻微阴影、透明度和留白。
6. 页面背景、卡片背景、边框和正文必须来自统一 token，禁止散落原始 hex。

### C. 布局与留白

1. 页面以“文字和空白的节奏”为主，不使用密集卡片网格主导首页。
2. 主内容宽度应克制，正文阅读行长建议约 60–80 个英文字符。
3. Hero 不得塞满技能标签、Logo 墙和统计卡片。
4. 首页首屏至少保留一个明显的大面积空白区。
5. 区块之间以 32px、48px、64px 级别的节奏拉开，不得平均分配成模板化 section。
6. 桌面端和移动端都必须保持相同排版气质，不能在移动端退化为普通卡片堆叠。
7. 圆角上限：Hero 16px，普通卡片 8px，标签 6px。

### D. 动效

统一使用接近以下曲线：

```css
cubic-bezier(0.22, 1, 0.36, 1)
```

要求：

1. 页面像展开一封信或翻开一页纸，而不是元素突然弹出。
2. 初次访问允许完整进入动画；回访应减少重复动画。
3. hover 仅轻微加深文字、改变透明度或产生极小位移。
4. 禁止弹跳式缩放、连续漂浮、满屏粒子、炫技视差。
5. 必须支持 `prefers-reduced-motion`。
6. 动效不得导致 CLS 或首屏内容延迟过久。

### E. 首页原型

首页至少实现：

- 姓名；
- 一句身份描述；
- 一段简洁个人介绍；
- 头像或明确占位；
- 精选项目/经历入口；
- 最近动态或最近文章区域；
- About / Projects / Writing / Contact 等基础导航；
- 深浅色切换；
- 中文为主，英文作为辅助；
- 页脚个人链接。

当真实资料尚未完整提供时：

- 使用 `[待补充：真实姓名]` 等明确占位；
- 不得使用 Innei 的文字；
- 不得编造项目和文章。

## Goal 0 验收标准

以下全部满足才算通过：

- [ ] 字体角色、字号 token、行高和字体 fallback 完整落地。
- [ ] 页面默认正文确实为 14px/22px，而非视觉上近似。
- [ ] 中文标题没有使用粗黑体。
- [ ] 浅色模式有纸张暖白感，深色模式为暖灰而非纯黑。
- [ ] 强调色占比受控，不超过约 5%。
- [ ] 首页无霓虹、无大渐变、无技能进度条、无密集 SaaS 卡片。
- [ ] 与 `innei.in` 并排时，字体层级、密度、留白和低噪声气质高度接近。
- [ ] 但没有复制 Innei 的个人文案、照片、Logo 或文章。
- [ ] 390px、768px、1024px、1440px 视口均无横向滚动和明显断裂。
- [ ] 键盘导航、可见焦点、语义化标题层级正常。
- [ ] `prefers-reduced-motion` 生效。
- [ ] `pnpm lint`、`pnpm test`、`pnpm build` 全部通过。
- [ ] 生成并保存首页浅色/深色、桌面/移动端共至少 4 张验收截图。

## Goal 0 失败返工规则

出现任一情况，Goal 0 立即判定失败并返工：

1. 页面看起来更像通用 Tailwind 模板，而不是 Yohaku 风格。
2. 字体选择随意，中文 fallback 缺失，或中文标题过粗。
3. 只改变颜色，却没有复现字号、行高、留白和排版密度。
4. 首页使用大量卡片、渐变、毛玻璃或发光效果掩盖排版不足。
5. 为了“像”而复制 Innei 的原文、照片、Logo 或私有资源。
6. 移动端只是桌面端机械堆叠。
7. 没有截图对比和视觉自检。
8. 构建失败或存在明显控制台错误。

返工时必须优先删除造成偏差的实现，而不是继续叠加 CSS 补丁。

## Goal 0 通过后

保留所有通过验收的：

- 设计 token；
- 字体角色；
- 字号与行高；
- 页面容器；
- 动效曲线；
- 深浅色体系；
- 基础导航和布局骨架。

删除：

- 所有明确占位的虚假内容；
- 未被使用的实验组件；
- 与 Yohaku 风格冲突的遗留样式；
- 临时调试面板、测试文案和重复 CSS。

随后进入 Goal 1。

---

# Goal 1 — 用真实简历重建个人信息架构

## Goal 定义

将用户提供的简历和素材转化为适合网站阅读的内容系统，而不是把 PDF 简历逐条搬上网页。

## 必须完成

1. 提取并结构化：
   - 教育背景；
   - 交换经历；
   - 实习/工作；
   - 研究；
   - 项目；
   - 奖项；
   - 技能；
   - 联系方式；
   - 文章或公开内容。
2. 为每条经历保留事实来源字段或注释。
3. 识别最强的 3–6 个核心项目。
4. 生成：
   - 首页短介绍；
   - About 长介绍；
   - 每段经历的一句话摘要；
   - 每个项目的问题、行动和结果。
5. 建立集中式内容数据层，页面不得散落硬编码个人内容。
6. 无真实图片时使用低调占位，不使用 Unsplash 假装成用户经历。

## Goal 1 验收标准

- [ ] 所有事实与简历一致。
- [ ] 没有编造内容。
- [ ] 首页没有堆完整简历。
- [ ] 访客在 10 秒内能理解用户是谁、在做什么、最强成果是什么。
- [ ] 所有 TODO 均集中记录。
- [ ] 内容结构支持中英文扩展。

## Goal 1 的删除/延续规则

通过后：

- 删除 Goal 0 的所有个人信息占位文案；
- 保留 Goal 0 的全部设计基线；
- 若真实内容造成版式失衡，只能调整内容节奏和局部布局，不得破坏字体 token 与核心 Yohaku 视觉规则。

未通过：

- 不得进入内部页面开发；
- 返回重做内容分类和首页信息层级。

通过后进入 Goal 2。

---

# Goal 2 — 完成首页与全站导航的高保真正式版

## Goal 定义

用真实内容完成一个足以直接对外发布的首页。首页应达到“不是模板、不是简历截图、不是 Innei 镜像，但视觉品质与排版气质明显对标 `innei.in`”的标准。

## 必须完成

- Hero；
- 当前身份与状态；
- 精选项目；
- 精选研究/经历；
- 最近文章或动态；
- 个人照片/头像；
- 联系入口；
- 页脚；
- 桌面和移动导航；
- 深浅色模式；
- 当前页面和 hover/focus 状态；
- 基础页面转场。

## Goal 2 验收标准

- [ ] 首屏信息层级清晰。
- [ ] 字体样式与 Goal 0 完全一致。
- [ ] 真实内容替换后仍保持充足留白。
- [ ] 不出现模板化技能矩阵。
- [ ] 关键链接全部有效。
- [ ] Lighthouse Performance、Accessibility、Best Practices、SEO 均达到 90+；如受外部资源影响，必须记录原因并尽量修复。
- [ ] 桌面和手机真机/模拟视口完成截图对比。

## Goal 2 的删除/延续规则

通过后：

- 删除未采用的 Hero 方案和重复组件；
- 保留正式首页作为后续所有页面的视觉标尺；
- 后续页面若与首页风格冲突，优先修改后续页面，禁止反向稀释首页风格。

未通过：

- 删除最偏离 Yohaku 的组件版本；
- 回到 Goal 0 的字体、留白和灰阶规则重新校准。

通过后进入 Goal 3。

---

# Goal 3 — 完成 About / Experience / Projects / Research / Education

## Goal 定义

把用户的核心经历组织成具有阅读节奏的个人档案，而不是多个同质化卡片页面。

## 页面要求

### About

- 个人背景；
- 兴趣交叉点；
- 当前关注；
- 少量生活化内容；
- 真实照片。

### Experience

- 时间线或分组阅读；
- 每段经历包含背景、行动、结果；
- 不照抄岗位职责。

### Projects

- 项目索引；
- 项目详情页；
- 背景、问题、角色、实现、结果、技术栈、链接、图片。

### Research

- 研究问题；
- 方法；
- 用户贡献；
- 当前状态；
- 论文/报告/代码链接；
- 面向非专业读者的解释。

### Education

- 学校、专业、交换、课程、荣誉；
- 可使用克制时间线，不做传统表格简历。

## Goal 3 验收标准

- [ ] 页面之间具有差异化内容结构，但共用同一设计语言。
- [ ] 无重复的大型卡片模板。
- [ ] 每页均有清晰 H1 和内容层级。
- [ ] 长文本阅读宽度和行高舒适。
- [ ] 图片均有尺寸、alt 和加载策略。
- [ ] 真实资料不足的页面自动隐藏，不展示空壳。

## Goal 3 的删除/延续规则

通过后：

- 删除空页面、假数据和没有实际内容的导航入口；
- 延续设计系统、导航、SEO 与内容数据层；
- 锁定项目详情模板，不得在后续 Goal 中随意重构。

未通过：

- 返回 Goal 1 重新梳理内容；
- 不得用更多装饰弥补内容层级问题。

通过后进入 Goal 4。

---

# Goal 4 — Writing / Notes / Journey 内容系统

## Goal 定义

建立类似长期个人空间的内容能力，使网站不仅是一张在线简历。

## 必须完成

- Markdown 或 MDX；
- 文章索引；
- 文章详情；
- 标题、摘要、发布时间、更新时间、标签；
- 代码高亮；
- 目录；
- 图片；
- 上一篇/下一篇；
- RSS；
- sitemap；
- 空状态；
- Journey/Moments 可选内容类型。

## 验收标准

- [ ] 没有假文章。
- [ ] 至少使用一篇用户提供的真实内容完成全链路验证；若没有真实文章，则保留系统并隐藏公开入口。
- [ ] 长文排版延续 serif、边注、纸张感和留白节奏。
- [ ] 代码块和中文正文均可读。
- [ ] 移动端目录和图片不破版。

## 删除/延续规则

通过后：

- 删除示例 lorem ipsum 和演示文章；
- 保留内容引擎；
- 隐藏没有真实内容的栏目。

未通过：

- 不得进入最终交付；
- 优先修复长文排版，而不是增加新功能。

通过后进入 Goal 5。

---

# Goal 5 — 发布级质量、视觉对比与最终收敛

## Goal 定义

将全站收敛到可公开发布状态，并证明它在视觉语言上高度接近参考目标，在内容与身份上完全属于用户本人。

## 必须完成

1. 全站视觉回归截图：
   - 390px；
   - 768px；
   - 1024px；
   - 1440px；
   - 浅色与深色。
2. 与 `innei.in` 参考截图进行人工对比，重点检查：
   - 字体材质；
   - 中文字重；
   - 字号与行高；
   - 留白；
   - 内容密度；
   - 灰阶；
   - 强调色占比；
   - hover 和页面进入节奏。
3. SEO：
   - metadata；
   - canonical；
   - Open Graph；
   - sitemap；
   - robots；
   - structured data；
   - favicon。
4. 工程质量：
   - lint；
   - test；
   - typecheck；
   - build；
   - 无明显 console error；
   - 无失效内部链接。
5. README：
   - 安装；
   - 开发；
   - 构建；
   - 部署；
   - 修改个人资料；
   - 添加项目；
   - 添加文章；
   - 替换图片；
   - 字体与设计 token；
   - 许可证说明。

## 最终验收标准

- [ ] 站点可稳定构建和部署。
- [ ] 所有公开内容真实。
- [ ] 视觉风格明显对标 `innei.in` / Yohaku。
- [ ] 字体系统是全站最强的一致性来源。
- [ ] 不包含 Innei 的个人资产和文字。
- [ ] 不像通用模板。
- [ ] 不像企业官网。
- [ ] 不靠发光、粒子和重型 3D 制造“炫酷”。
- [ ] 页面在手机端仍有纸张感与留白节奏。
- [ ] Lighthouse 四项 90+，或有书面说明的合理例外。
- [ ] README 足以让新维护者独立更新内容。

## Goal 5 失败处理

如最终视觉与参考风格差距明显：

1. 不增加新功能；
2. 冻结内容；
3. 回到 Goal 0；
4. 优先重做字体、字号、行高、容器宽度和留白；
5. 删除所有掩盖排版问题的装饰；
6. 重新生成对比截图；
7. 直到通过后再结束项目。

---

# 一次性连续执行状态机

Codex 必须把本文件视为一个**不可中途停止的完整交付任务**，而不是需要用户逐阶段批准的待办列表。

```text
读取仓库、简历、素材与参考站
  ↓
Goal 0：建立视觉基线
  ├─ fail → 当前任务内删除偏差实现并返工，直到 pass
  └─ pass → 自动进入 Goal 1

Goal 1：重建真实内容结构
  ├─ fail → 当前任务内重组内容，直到 pass
  └─ pass → 自动进入 Goal 2

Goal 2：完成正式首页
  ├─ fail → 当前任务内回退校准视觉，直到 pass
  └─ pass → 自动进入 Goal 3

Goal 3：完成核心内部页面
  ├─ fail → 当前任务内重构页面或内容，直到 pass
  └─ pass → 自动进入 Goal 4

Goal 4：完成 Writing / Journey 内容能力
  ├─ fail → 当前任务内修复，直到 pass
  └─ pass → 自动进入 Goal 5

Goal 5：全站质量收敛
  ├─ fail → 当前任务内继续返工，不得停止
  └─ pass → 构建生产版本 → 部署 → 验证线上站点 → 返回公开 URL
```

## 严格连续执行规则

1. 不得在 Goal 0、Goal 1 或任何中间阶段结束回复。
2. 不得输出“请确认后我再继续”“下一步可以做”“当前只能执行 Goal X”。
3. 不得把阶段性截图、分析报告或 TODO 清单当成交付结果。
4. 阶段验收只是 Codex 的内部质量门槛，不是暂停点。
5. 发现问题必须直接修复；缺少非关键素材时使用明确、低调、可替换的占位，并继续完成网站。
6. 只有遇到完全无法绕过的外部权限问题时，才允许说明阻塞；在此之前必须完成所有可完成工作。
7. 若仓库已有部署平台配置，优先沿用；否则优先部署到 Vercel。若 Vercel 不可用，可使用仓库现有支持的其他公开部署平台。
8. 部署后必须实际访问公开 URL，检查首页、导航、移动端、深色模式、关键页面和静态资源是否正常。

## 唯一完成定义（Definition of Done）

本任务只有在以下条件全部满足时才允许结束：

- 全部 Goal 0–5 已连续完成并通过；
- 项目可成功 lint、typecheck、test、build；
- 网站已部署到公开可访问的地址；
- 已实际打开线上地址并验证页面不是 404、空白页或构建错误；
- 用户提供的真实简历内容已进入网站；
- `innei.in` / Yohaku 的字体气质、字号层级、暖灰纸张感、留白和克制动效得到高保真复现；
- 没有复制 Innei 的个人文字、头像、Logo、照片或受保护的私人资源；
- 最终回复首要内容是一个可直接点击的线上链接。

若尚未产生可访问链接，任务即视为**未完成**，不得以本地截图、代码说明或“部署步骤”代替。

---

# 禁止事项

- 禁止把“参考 `innei.in`”弱化成普通极简风。
- 禁止仅复制颜色，不复制字体层级和留白节奏。
- 禁止大面积渐变、霓虹、发光边框和粒子背景。
- 禁止技能百分比进度条。
- 禁止密集 Bento Grid 统治首页。
- 禁止用大量毛玻璃卡片制造所谓高级感。
- 禁止复制 Innei 的个人内容、头像、Logo 和私有代码。
- 禁止虚构用户个人信息。
- 禁止在未通过当前 Goal 时越级开发。
- 禁止只写问题报告而不修复。
- 禁止为赶进度牺牲字体与移动端排版。

---

# 当前执行指令

现在立即从 Goal 0 开始，并在**本次任务中连续执行 Goal 0 → Goal 1 → Goal 2 → Goal 3 → Goal 4 → Goal 5 → 部署验证**。

不要停在任何中间 Goal，不要向用户申请阶段性确认。内部验收失败就直接返工，直到满足标准。

最终只在以下内容全部准备好后回复用户：

1. **公开可访问的网站链接**（放在回复第一行）；
2. 源代码仓库或提交信息；
3. 一句说明已经完成哪些页面；
4. 构建、测试和线上验证结果；
5. 少量仍需用户后续替换的真实素材（如确实缺失）。

最终回复不得以 Goal 0 报告、设计分析或本地运行说明结束。没有公开链接，就继续开发、修复和部署。
